{
  "name": "ai-todo-app",
  "version": "1.0.0",
  "description": "AI-Driven Smart Task Planner - Full Stack Application",
  "scripts": {
    "build": "cd Frontend && npm install && npm run build",
    "dev": "cd Frontend && npm run dev"
  },
  "dependencies": {
    "@google/generative-ai": "^0.2.1",
    "mongoose": "^8.0.3",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": "18.x"
  }
}

